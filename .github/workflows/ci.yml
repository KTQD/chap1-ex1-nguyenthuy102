name: C Program Tests

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out code
      uses: actions/checkout@v2

    - name: Build and test
      run: |
        gcc -o rectangle rectangle.c
        ./rectangle << EOF
        5
        7
        EOF
